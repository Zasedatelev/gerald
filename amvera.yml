# amvera.yml — конфиг для БЭКЕНД-проекта на Amvera
# Amvera подключает этот репо и читает Dockerfile из корня

meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  containerPort: 8000
  persistenceMount: /data
